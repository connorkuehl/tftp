version: "3"
services:
    test:
        build:
            context: .
            dockerfile: ./dockerfiles/Dockerfile
        ports:
            - 6655:6655
        volumes:
            - ./artifacts:/usr/bin/artifacts
